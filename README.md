# LSP Syntax Highlighter

This repository contains code for the lsp-syntax-highlighter package (in the highlighter folder) and the lsp-syntax-highlighter-compiler package (in the compiler folder). Please see the README files in those folders for docs.