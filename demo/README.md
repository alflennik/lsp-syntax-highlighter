# Demo

## Setup

To view the demo, open a terminal in the demo folder.

Run `npm install`

Run `node generateDemo.js`

Run a basic web server (I recommend the "Open in Default Browser" VSCode extension) and open index.html.

Hold shift to preview before and after. Try shift + right and shift + down to quickly cycle through the themes and grammars.